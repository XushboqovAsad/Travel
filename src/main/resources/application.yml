server:
  port: 8084
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/thirded
    username: thirded
    password: 123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
